using System;

namespace OOP_Babini
{
    internal class WeaponFactory
    {
        internal static Weapon createRandomWeaponLevel(object p1, Maps map, object p2)
        {
            return new Weapon();
        }
    }
}